<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:localCommands="clr-namespace:Presentation.Commands">
    
    <Style TargetType="Button" x:Key="GroupButtonStyle">
        <Style.Setters>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF636363" />
            <Setter Property="Foreground" Value="White"/>

            <Setter Property="Width">
                <Setter.Value>
                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}"/>
                </Setter.Value>
             </Setter>

            <Setter Property ="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                CornerRadius="7"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                Height="{TemplateBinding Height}"
                                Width="{TemplateBinding Width}" >
                                <ContentControl Margin="{TemplateBinding Padding}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Content="{TemplateBinding Content}" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="Wheat"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="Red"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style.Setters>
    </Style>
    
    <DataTemplate x:Key="GroupItemTemplate">
        <Button Style="{StaticResource ResourceKey=GroupButtonStyle}"
                Command="localCommands:WindowCommands.LoadGroup"
                CommandParameter="{Binding}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Path=Number}"/>
                <TextBlock Text="{Binding Path=Letter}"/>
            </StackPanel>
        </Button>
    </DataTemplate>
</ResourceDictionary>