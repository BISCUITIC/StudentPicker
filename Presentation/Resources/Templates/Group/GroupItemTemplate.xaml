<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Styles/Buttons/GroupButtonStyle.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Key="GroupItemTemplate">
        <Button Style="{StaticResource ResourceKey=GroupButtonStyle}"
                Command="{Binding DataContext.StudentsViewModel.LoadStudentsCommand,
                                  RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="{Binding Path=Group}">

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Path=Group.Number}"/>
                <TextBlock Text="{Binding Path=Group.Letter}"/>
            </StackPanel>
            
            <Button.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding DeleteGroupCommand}"
                              Header="Delete"/>
                    <MenuItem Command="{Binding UpdateGroupCommand}" 
                              Header="Edit"/>
                </ContextMenu>
            </Button.ContextMenu>
            
        </Button>
                        
    </DataTemplate>
    
</ResourceDictionary>