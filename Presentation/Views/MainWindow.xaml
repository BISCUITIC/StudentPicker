<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        mc:Ignorable="d"
        Style="{StaticResource ResourceKey = WindowStyle}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <ItemsControl Grid.Row="0" x:Name="GroupList"
                      Background="#FF2E2E2E"
                      ItemsSource="{Binding Groups}"
                      ItemsPanel="{StaticResource ResourceKey=GroupItemPanel}"
                      ItemTemplate="{StaticResource ResourceKey=GroupItemTemplate}">
        </ItemsControl >
        
        <Button Grid.Row="2" x:Name="RandomButton"                 
                Style="{StaticResource ResourceKey = RandomButtonStyle}"/>
    </Grid>
</Window>
